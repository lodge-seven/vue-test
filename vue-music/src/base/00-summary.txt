api存放一些封装类，不是组件，是通用的js操作，比如这里因为常用到解析jsonp，所以把它封装成工厂模式对象。（误）
好吧 逻辑重新梳理

因为是常用到jsonp的方法，所以进行了封装，这个纯js必须是不带业务逻辑的，只暴露基本的接口，返回的也是确定的类型，所以放入了common通用功能区中，给大家调用，也方便重构。

在api中进行jsonp调用，又封装成一个函数，这是把基础组件与其业务区分，vm视图中业务的部分剥离开，只留下调用的接口和前端vm的逻辑

在base中创建组件，这个组件又是可利用的，或者说可以改动的部分

components是主要的基调，整体的前端业务逻辑